import React from "react";
import BeatLoader from "react-spinners/BeatLoader";

// eslint-disable-next-line import/no-anonymous-default-export
export default () => (
  <BeatLoader color={"red"} size={20} speedMultiplier={0.5} />
);
